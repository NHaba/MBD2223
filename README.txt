
Para construir la imagen, usar:
docker build -t hola-javac // Para construir este docker

Para ejecutar el contenedor, ejecuta el siguiente comando:
docker run -v "<path>":/app/ <container>



# docker images // Para ver las imagenes que hay
# docker run hola-javac
